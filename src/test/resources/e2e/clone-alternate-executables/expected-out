  mkdir -p ${HOME}/cvs
  cd ${HOME}/cvs
  /usr/local/bin/cvs -d :ext:example.com:/nonexistent/cvsroot checkout -P -ko mod
  mkdir -p ${HOME}/git
  cd ${HOME}/git
  ~/bin/my-git clone -- https://github.com/plume-lib/options.git options
  mkdir -p ${HOME}/hg
  cd ${HOME}/hg
  hg-3 clone https://www.mercurial-scm.org/repo/hg hg
  mkdir -p ${HOME}/svn
  cd ${HOME}/svn
  /opt/svn/bin/svn checkout svn+ssh://example.com/repos/proj
  mkdir -p ${HOME}/svn
  cd ${HOME}/svn
  /opt/svn/bin/svn checkout svn+ssh://example.com/repos/proj2
