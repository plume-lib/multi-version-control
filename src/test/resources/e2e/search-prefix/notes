--search-prefix also processes every sibling directory whose name starts with the name of a
directory in the configuration file.

The output is sorted before it is compared, because the program emits these clones in the order
that File.listFiles returns them, which is the order of the underlying file system.  The program
ought to sort them, as the --search code does.
